# AGENTS.md

项目处于快速迭代阶段，只保留确保开发方向不跑偏的硬性约定。

## 技术栈

- 前端界面 Gradio；
- 后端 openLLV [用法参考](https://github.com/glory-wan/openLLV/tree/main/docs)；
- 包管理 uv；
- 配置 PyYAML 读 `cfg/*.yaml`；
- lint 用 ruff

## 硬性规则

1. 三大功能各占一个包：推理、训练、评估。每个包内界面组装与业务方法分开：界面组装函数 `build()` 负责渲染组件、绑定事件，并从同包引用业务方法 `run_*`，业务方法不写界面。
2. 入口只组装：顶层入口只建 `gr.Blocks` + 三个 `gr.Tab`，调用各功能的 `build()`，不写业务逻辑。
3. 复用 openLLV，不自造逻辑：图像读写、预测、训练、评估全部走 `openLLV` 公开 API。
4. 不得复刻 openLLV 的校验：凡调用 openLLV 接口的场景，不自写参数校验、路径检查；非法输入让 openLLV 自己 `raise`，异常原样透传给用户。仅与 openLLV 无关的纯界面级判断用 `gr.Error`。
5. 页面可选项从配置来：放 `cfg/*.yaml`，由 `app` 顶层加载导出大写常量，页面/业务模块从 `app` 顶层导入，不在页面模块硬编码。
6. 测试：仅对与 openLLV 接口无关的自写逻辑补少量关键逻辑的 pytest 用例；纯透传 openLLV 的业务方法不写测试。
7. 注释：函数、界面模块、较长逻辑要加注释，长逻辑分步说明；注释只讲用法/作用，不讲规定。

## 边界

- 单页单模块即可，不要为了"架构"引入额外抽象层。
- 单机单用户，无登录、无任务队列、无异步。
